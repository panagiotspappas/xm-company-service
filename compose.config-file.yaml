services:
  company-service:
    volumes:
      - type: bind
        source: ${CONFIG_FILE:?CONFIG_FILE must name a host configuration file}
        target: /app/config.json
        read_only: true
    environment:
      CONFIG_FILE: /app/config.json
