diff --git a/cmd/company-service/main.go b/cmd/company-service/main.go
index 055958c..e9ccfe2 100644
--- a/cmd/company-service/main.go
+++ b/cmd/company-service/main.go
@@ -50,7 +50,7 @@ func run() error {
 
 	repository := postgresrepository.NewRepository(pool)
 	service := company.NewService(repository, uuid.NewRandom)
-	tokenValidator := auth.NewValidator(cfg.JWTSecret, cfg.JWTIssuer, cfg.JWTAudience)
+	tokenValidator := auth.NewValidator(cfg.JWT.Secret, cfg.JWT.Issuer, cfg.JWT.Audience)
 	handler := httpapi.NewRouter(service, httpapi.RequireAuthentication(tokenValidator))
 	server := &http.Server{
 		Addr:    cfg.HTTPAddress,
diff --git a/internal/config/config.go b/internal/config/config.go
index ded5332..afb25bf 100644
--- a/internal/config/config.go
+++ b/internal/config/config.go
@@ -29,9 +29,7 @@ type Config struct {
 	DatabaseURL string
 	LogLevel    slog.Level
 	LogFormat   LogFormat
-	JWTSecret   string
-	JWTIssuer   string
-	JWTAudience string
+	JWT         JWTConfig
 }
 
 // JWTConfig contains the configuration shared by JWT verification and
@@ -69,9 +67,7 @@ func Load() (Config, error) {
 		DatabaseURL: databaseURL,
 		LogLevel:    logLevel,
 		LogFormat:   logFormat,
-		JWTSecret:   jwtConfig.Secret,
-		JWTIssuer:   jwtConfig.Issuer,
-		JWTAudience: jwtConfig.Audience,
+		JWT:         jwtConfig,
 	}, nil
 }
 
diff --git a/internal/config/config_test.go b/internal/config/config_test.go
index c5ec737..6a42d49 100644
--- a/internal/config/config_test.go
+++ b/internal/config/config_test.go
@@ -44,14 +44,14 @@ func TestLoadDefaults(t *testing.T) {
 	if got.LogFormat != LogFormatText {
 		t.Fatalf("LogFormat = %q, want %q", got.LogFormat, LogFormatText)
 	}
-	if got.JWTSecret != testJWTSecret {
-		t.Fatal("JWTSecret was not loaded")
+	if got.JWT.Secret != testJWTSecret {
+		t.Fatal("JWT.Secret was not loaded")
 	}
-	if got.JWTIssuer != testJWTIssuer {
-		t.Fatalf("JWTIssuer = %q, want %q", got.JWTIssuer, testJWTIssuer)
+	if got.JWT.Issuer != testJWTIssuer {
+		t.Fatalf("JWT.Issuer = %q, want %q", got.JWT.Issuer, testJWTIssuer)
 	}
-	if got.JWTAudience != testJWTAudience {
-		t.Fatalf("JWTAudience = %q, want %q", got.JWTAudience, testJWTAudience)
+	if got.JWT.Audience != testJWTAudience {
+		t.Fatalf("JWT.Audience = %q, want %q", got.JWT.Audience, testJWTAudience)
 	}
 }
 
@@ -80,11 +80,11 @@ func TestLoadConfiguredValues(t *testing.T) {
 	if got.LogFormat != LogFormatJSON {
 		t.Fatalf("LogFormat = %q, want %q", got.LogFormat, LogFormatJSON)
 	}
-	if got.JWTIssuer != testJWTIssuer {
-		t.Fatalf("JWTIssuer = %q, want trimmed value %q", got.JWTIssuer, testJWTIssuer)
+	if got.JWT.Issuer != testJWTIssuer {
+		t.Fatalf("JWT.Issuer = %q, want trimmed value %q", got.JWT.Issuer, testJWTIssuer)
 	}
-	if got.JWTAudience != testJWTAudience {
-		t.Fatalf("JWTAudience = %q, want trimmed value %q", got.JWTAudience, testJWTAudience)
+	if got.JWT.Audience != testJWTAudience {
+		t.Fatalf("JWT.Audience = %q, want trimmed value %q", got.JWT.Audience, testJWTAudience)
 	}
 }
 
